Start backend yourself: cd backend &&
 .venv\Scripts\uvicorn app.main:app --reload --host 0.0.0.0 --port 8000

Start Celery yourself (in another terminal): cd backend &&
 .venv\Scripts\celery -A app.worker worker --loglevel=info --pool=solo
 
Start frontend (in another terminal): cd frontend && npm run dev